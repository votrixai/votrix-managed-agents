[project]
name = "votrix-managed-agents"
version = "0.1.0"
description = "Self-hosted, multi-tenant managed-agents control plane powered by Deep Agents."
requires-python = ">=3.12"
license = "LicenseRef-Proprietary"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.30",
    "sqlalchemy[asyncio]>=2.0",
    "asyncpg>=0.29",
    "aiosqlite>=0.20",
    "alembic>=1.13",
    "pydantic>=2.8",
    "pydantic-settings>=2.0",
    "python-dotenv>=1.0",
    "structlog>=24.0",
    "sentry-sdk[fastapi]>=2.0",
    "httpx>=0.27,<0.29",
    "httpcore>=1.0.5,<1.1",
    "aioboto3>=13.0",
    "cryptography>=42.0",
    "deepagents==0.6.12",
    "langchain-mcp-adapters>=0.3.0,<1.0.0",
    "langgraph-checkpoint-postgres>=3.1.0,<4.0.0",
    "psycopg[binary,pool]>=3.3.4,<4.0.0",
    "langgraph-checkpoint-sqlite>=3.1.0,<4.0.0",
    "langchain-openrouter>=0.2.6,<1.0.0",
    "google-cloud-tasks>=2.19,<3.0",
    "e2b==2.31.0",
    "langchain-e2b==0.0.5",
]

[project.scripts]
vma-worker = "app.worker:main"

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "pytest-asyncio>=0.23",
    "httpx>=0.27",
]
sandbox-e2b = [
    "langchain-e2b==0.0.5",
    "e2b==2.31.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["app", "votrix_managed_agents"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
markers = [
    "contract: compatibility tests that require official third-party SDK clients",
    "postgres: integration tests that require an isolated PostgreSQL database",
]
