---
title: API Reference
description: Historical API reference generated from the pre-rewrite application.
---

<Callout title="Pre-rewrite schema" type="warn">

This committed API reference was generated from the archived application. It is
not the API implemented by the active `app/` rewrite and has been removed from
the current documentation navigation.

</Callout>

Do not use the generated playground against a live service. The checked-in
`public/openapi/vma.json` and its endpoint pages contain the historical API-key,
Vault, provider, quota, and deployment surface.

Start the active service with:

```bash
uv run uvicorn app.main:app --reload
```

Then read its current schema from:

```text
http://127.0.0.1:8000/openapi.json
```

The documentation exporter must be ported to `app.server.create_app` before
this generated reference can become current again.
